{
  "name": "claude-desktop-win-zh-cn",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:ui": "npm run dev --prefix ui",
    "build:ui": "npm run build --prefix ui",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build --no-bundle"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
